---
layout: destination-hub.njk
title: Discover the World's Finest Luxury Destinations
pagination:
  data: collections.destinations
  size: 30
  alias: destinationsPage
eleventyExcludeFromCollections: true
---

   <!-- Top Destinations of the Year -->
   <section class="max-w-1100 mt-40 pt-50 pb-50 mb-20">

     <h2 class="pl-4">Top Luxury Destinations of the Year</h2>
     <p class="pl-4">
       The most loved, most searched, and most exclusive destinations this year.
     </p>

     <div class="featured-destinations">
       <div class="swiper categories-swiper">
         <div class="swiper-wrapper">

           {% for dest in collections.featuredDestinations %}
             {% include "blocks/featured-destinations.njk" %}
           {% endfor %}

         </div>
        </div>

       <div class="swiper-button-next"></div>
       <div class="swiper-button-prev"></div>
     </div>
   </section>


   <!-- All Destinations -->
   <section class="max-w-1300 w-full mb-60 p-20">

     <h2>All Luxury Destinations</h2>
     <p>Browse every destination across continents and countries.</p>

     <div class="flex gap-1-5 flex-wrap justify-center">
       {% for dest in destinationsPage %}
         <article class="destination-cards mt-20">
           <img
             src="{{ dest.data.image }}"
             alt="{{ dest.data.title }}"
             width="600"
             height="400"
             loading="lazy"
           />
           <h3>{{ dest.data.title }}</h3>
           <p>{{ dest.data.excerpt }}</p>
           <a href="{{ dest.url }}" class="btn3">View destination</a>
         </article>
       {% endfor %}
     </div>


     <nav class="pagination">

       {% if pagination.href.previous %}
         <a href="{{ pagination.href.previous }}">← Previous</a>
       {% endif %}

       <span>
         Page {{ pagination.pageNumber + 1 }} of {{ pagination.pages | length }}
       </span>

       {% if pagination.href.next %}
         <a href="{{ pagination.href.next }}">Next →</a>
       {% endif %}

     </nav>
   </section>

   <!-- featured Experiences -->
   <section class="mt-70 pt-50 mb-60 pb-50 max-w-1100">
     <div class="p-20 max-w-1100">
        <h2>Luxury Tours & Experiences</h2>
        <p>
          Discover curated, high-end experiences
          designed for travelers who demand exclusivity, privacy, and
          unforgettable moments.
        </p>
      </div>

      <div class="deals">
        {% for exp in collections.featuredExperiences %}
          {% include "blocks/featured-experience.njk" %}
        {% endfor %}
      </div>
      <div class="flex gap-10 flex-wrap w-30r m-3 pl-1-5">
        <a href="/experiences" class="btn2 color-dark">
          Explore more
        </a>
      </div>
    </section>

   <!-- featured Blogs -->
   <section class="max-w-1100 mt-60 mb-60">
     <h2>Luxury Travel Guides & Insights</h2>
     <p>Expert advice, itineraries, and destination inspiration.</p>

     <div class="featured-destinations">
       <div class="swiper categories-swiper">
         <div class="swiper-wrapper">

           {% for post in collections.featuredBlogs %}
             {% include "blocks/blog-cards.njk" %}
           {% endfor %}

         </div>
       </div>

       <div class="swiper-button-next"></div>
       <div class="swiper-button-prev"></div>
     </div>
   </section>





